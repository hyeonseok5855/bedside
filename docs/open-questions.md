# Open Questions

구현 전에 답해야 하는, 아직 어느 문서에도 결정으로 잡히지 않은 질문들.
이미 `interviewer.md`·`voice.md` 등의 "남은 것/확인 필요"에 있는 항목은 여기
중복해 넣지 않는다. 여기 있는 것은 **비면 코드가 막히거나(A), 나중에 넣으면
마이그레이션 지옥인 기반(B)** 위주다.

답이 정해지면 `decisions.md`로 옮기고 여기서는 지운다.

작성일: 2026-07-27

## 상태: 초기 미결 질문 전부 해결됨 (2026-07-27)

처음 뽑은 13개 질문(A1~F14)은 모두 결정으로 옮겼다. 새 질문이 생기면 여기에
다시 쌓는다.

| 질문 | 결정 |
|---|---|
| 일기 생성 시점·주체, 무응답 타임아웃 처리 | **13** |
| 오디오 경로(스피커 반이중), barge-in/AEC 범위 | **14** |
| SQLCipher 키 위치·위협 모델 | **15** |
| 이어하기 의미론(어제 닫고 오늘 새로) | **16** |
| 백그라운드 수집 생존 전략(foreground service 상주) | **17** |
| API 키 위치(APK BuildConfig) | **18** |
| 화면 잠금 중 대화(wake lock, 검은 화면) | **19** |
| 밤 준비 트리거(귀가 기반 적응형) | **20** |
| egress 인벤토리·실명 전송·export 키 이원화 | **21** (`egress.md`) |
| 인물 이름 STT 오인식 교정 | **22** |
| 문체 레퍼런스: 수정본 vs 원본 | **23** |
| "대화가 성립한 날" 정의 | **24** |
| 일일 상한 도달 시 대화 중 동작 | **25** |

## 아직 다른 문서에 남아 있는 확인 항목 (여기 중복 안 함)

- `voice.md`: gRPC 스트리밍 키워드 부스팅 지원 여부, EOT 내장 여부 → VAD
  직접 구현 필요성.
- `interviewer.md`: 말투 예시 few-shot 확정, 브리핑 주입 포맷, 프로필 주입
  위치·분량 상한.
- `diary-writer.md`: 제목/소제목 vs 통짜 산문, 일기↔transcript 점프, 사진
  삽입 위치 결정 주체, 레퍼런스 편수·토큰 예산.
- `decisions.md` 10: 개발용 adb MCP 연동(미착수).
