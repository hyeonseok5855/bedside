<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Claude API 호출 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Health Connect 읽기 (v1 수집 소스: 수면·걸음·몸무게) -->
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />

    <!-- 사진 메타데이터 읽기 (MediaStore). v1은 메타데이터만, 이미지 자체는 안 읽는다. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- 사진 EXIF의 GPS를 읽기 위해 필요(setRequireOriginal). -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- 위치/지오펜스 (집·회사 진입·이탈 로깅) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 앱이 꺼져 있어도 지오펜스가 발화하려면 필요(Android 10+에서 별도 허용) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- 진입·이탈 알림 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 오늘 일정 파악 (삼성 캘린더 포함, 표준 CalendarContract). 결정 38 -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- 수집 서비스 (FGS) + 재부팅 재등록 + 배터리 최적화 예외 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Health Connect 제공자(앱) 설치 여부를 조회하기 위한 package visibility -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:name=".BedsideApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bedside">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Health Connect 권한 근거 화면 (Android 13 이하) -->
            <intent-filter>
                <action android:name="androidx.health.connect.action.HEALTH_CONNECT_PERMISSIONS" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ConversationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".DiaryViewerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ReviewActivity"
            android:exported="false" />

        <activity
            android:name=".SearchActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 지오펜스 전이(진입·이탈) 수신 -->
        <receiver
            android:name=".location.GeofenceBroadcastReceiver"
            android:exported="false" />

        <!-- 수집 서비스 및 스케줄/부팅 리시버 -->
        <service
            android:name=".collect.CollectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".collect.CollectionAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".collect.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Health Connect 권한 근거 화면 (Android 14+) -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
    </application>
</manifest>
